nextflow_pipeline {

    name "Test ncbi_ascp"
    script "workflows/ncbi.nf"

    test("Should run without failures") {

        when {
            params {
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.stdout).match()
        }

    }

}
