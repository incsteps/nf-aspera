nextflow_pipeline {

    name "Test ena_ascp"
    script "workflows/ena.nf"

    test("Should run without failures") {

        when {
            params {
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.stdout).match()
        }

    }

}
